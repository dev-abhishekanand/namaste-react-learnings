# Namaste React Learnings

#Redux
-install rdux toolkit and react-redux
-build our store
-connect store our app
-create a cart slice
-dispatch(action)
-using reducer function update the slice
-selector(subscribe) to update cart in header
